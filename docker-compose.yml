services:
  backend:
    build: ./backend
    container_name: amaya-backend
    restart: always
    environment:
      NODE_ENV: production
      PORT: 5000
      MONGODB_URI: mongodb+srv://admin_news:AxioNews2026@cluster0.qlfjmpu.mongodb.net/amaya_news?retryWrites=true&w=majority&authSource=admin
      JWT_SECRET: super_secret_key_change_me_2024
      EMAIL_USER: arthelp2024@gmail.com
      EMAIL_PASS: ubraaquqcybrgshm
      CORS_ORIGIN: https://amayanews.com
    volumes:
      - ./uploads:/app/uploads

  frontend:
    build: ./frontend
    container_name: amaya-frontend
    restart: always

  nginx:
    image: nginx:alpine
    container_name: amaya-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - backend
      - frontend

volumes:
  uploads:
